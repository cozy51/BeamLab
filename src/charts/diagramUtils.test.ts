import{describe,expect,it}from'vitest';import{diagramTextAnchor,rotationDirection,signedNumber,verticalDirection}from'./diagramUtils';
describe('load diagram signs',()=>{it('maps vertical force signs',()=>{expect(verticalDirection(1)).toBe('up');expect(verticalDirection(-1)).toBe('down');expect(verticalDirection(0)).toBe('zero')});it('maps moment signs',()=>{expect(rotationDirection(1)).toBe('counterclockwise');expect(rotationDirection(-1)).toBe('clockwise')});it('formats explicit positive sign',()=>{expect(signedNumber(10,String)).toBe('+10');expect(signedNumber(-10,String)).toBe('-10')});it('keeps edge labels inside the diagram',()=>{expect(diagramTextAnchor(0,1000)).toBe('start');expect(diagramTextAnchor(500,1000)).toBe('middle');expect(diagramTextAnchor(1000,1000)).toBe('end')})});