import {useState} from 'react';import type{ResultPoint}from'../types/beam';import{formatNumber}from'../utils/units';
type Key='shear'|'moment'|'deflection';
export function EngineeringChart({points,dataKey,title,unit,color}:{points:ResultPoint[];dataKey:Key;title:string;unit:string;color:string}){const[hover,setHover]=useState<ResultPoint>();const W=760,H=245,pad=46,vals=points.map(p=>p[dataKey]),max=Math.max(...vals.map(Math.abs),1e-12),L=points.at(-1)?.x||1,scale=dataKey==='deflection'?Math.max(1,10**Math.floor(Math.log10(L*.08/max))):1,sx=(x:number)=>pad+x/L*(W-2*pad),sy=(v:number)=>H/2-v/max*(H/2-pad),path=points.map((p,i)=>`${i?'L':'M'}${sx(p.x)},${sy(p[dataKey])}`).join(' '),ext=points.reduce((a,p)=>Math.abs(p[dataKey])>Math.abs(a[dataKey])?p:a);return <section className="chart-card"><div className="chart-title"><strong>{title}</strong><div className="chart-summary" style={{borderLeftColor:color}}><span className="summary-label">最大絶対値</span><b>{formatNumber(Math.abs(ext[dataKey]))}<em>{unit}</em></b><span className="summary-position">発生位置 x = {formatNumber(ext.x)} mm</span>{dataKey==='deflection'&&<span className="summary-scale">変形表示倍率 ×{formatNumber(scale,0)}（自動）</span>}</div></div><svg viewBox={`0 0 ${W} ${H}`} onMouseLeave={()=>setHover(undefined)} onMouseMove={e=>{const r=e.currentTarget.getBoundingClientRect(),x=Math.max(0,Math.min(L,(e.clientX-r.left)/r.width*W-pad))*L/(W-2*pad);setHover(points.reduce((a,p)=>Math.abs(p.x-x)<Math.abs(a.x-x)?p:a))}}><line x1={pad} y1={H/2} x2={W-pad} y2={H/2} className="axis"/><line x1={pad} y1={pad/2} x2={pad} y2={H-pad/2} className="axis"/><path d={path} fill="none" stroke={color} strokeWidth="2.5"/><circle cx={sx(ext.x)} cy={sy(ext[dataKey])} r="5" fill={color}/>{hover&&<><line x1={sx(hover.x)} y1="15" x2={sx(hover.x)} y2={H-15} className="cursor"/><g transform={`translate(${Math.min(sx(hover.x)+8,W-245)},18)`}><rect width="235" height="136" rx="6" className="tooltip-bg"/><text x="12" y="22">x = {formatNumber(hover.x)} mm</text><text x="12" y="43">V = {formatNumber(hover.shear)} N</text><text x="12" y="64">M = {formatNumber(hover.moment)} N·mm</text><text x="12" y="85">σ = {formatNumber(hover.stress)} MPa</text><text x="12" y="106">θ = {formatNumber(hover.rotation,6)} rad</text><text x="12" y="127">δ = {formatNumber(hover.deflection,6)} mm</text></g></>}</svg></section>}