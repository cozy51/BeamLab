import{describe,expect,it}from'vitest';import{sectionProperties,validateSection}from'./properties';describe('sections',()=>{it('rectangle',()=>{const p=sectionProperties({type:'rectangle',b:20,h:40});expect(p.area).toBe(800);expect(p.inertia).toBeCloseTo(20*40**3/12);expect(p.modulus).toBeCloseTo(20*40**2/6)});it('circle',()=>{const p=sectionProperties({type:'circle',d:20});expect(p.inertia).toBeCloseTo(Math.PI*20**4/64);expect(p.modulus).toBeCloseTo(Math.PI*20**3/32)});it('rejects invalid tube',()=>expect(validateSection({type:'hollow-circle',D:20,d:20}).length).toBeGreaterThan(0))});