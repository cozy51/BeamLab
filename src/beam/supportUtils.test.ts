import{describe,expect,it}from'vitest';import{nextSupportPosition}from'./supportUtils';describe('support editor placement',()=>{it('adds into the widest available interval',()=>{expect(nextSupportPosition(1000,[{id:'a',name:'A',position:0,type:'pin'},{id:'b',name:'B',position:1000,type:'roller'}])).toBe(500);expect(nextSupportPosition(1000,[{id:'a',name:'A',position:0,type:'pin'},{id:'b',name:'B',position:500,type:'roller'},{id:'c',name:'C',position:1000,type:'roller'}])).toBe(250)})});